# Secrets & environment — NEVER commit these (the auto-push hook also refuses them)
.env
.env.*
*.env
secrets*
*.pem
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Node / frontend
node_modules/
dist/
.vite/
*.log

# OS / editor
.DS_Store
.idea/
.vscode/

# Large demo media (keep fallbacks out of git unless you decide otherwise)
# /fallbacks/
