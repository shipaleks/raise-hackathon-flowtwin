# TFT forecast pipeline (Path B — local CPU/MPS, no GPU or docker).
# python 3.12.  install:  python3.12 -m venv .venv-tft && .venv-tft/bin/pip install -r tft/requirements.txt
torch==2.12.1
pytorch-forecasting==1.8.0
lightning==2.6.5
pandas==3.0.3
pyarrow==24.0.0
numpy==2.5.1
